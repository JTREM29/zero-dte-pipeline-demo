[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
asyncio_mode = auto
timeout = 60
addopts = -v --tb=short

[tool:pytest]
filterwarnings =
    ignore::DeprecationWarning
