# Core scientific stack
pandas==2.3.2
numpy==2.3.3
scipy>=1.11.0
pyarrow>=17.0.0

# Data & APIs (uncomment / add as you integrate)
openai==1.109.1
openai-agents==0.3.1
websocket-client==1.8.0
httpx>=0.28.1
# polygon-api-client
# iqfeed

# Config & validation
pydantic>=2.6.0
pydantic-settings>=2.6.0
python-dotenv>=1.0.0
tzdata>=2024.1
pytz>=2024.1

# CLI / Tooling
typer>=0.12.0
rich>=13.7.0

# Scheduling / pipelines (optional candidates)
# prefect
# airflow

# ML / Modeling (add when needed)
# scikit-learn
# xgboost
autogluon.timeseries>=1.4.0  # Chronos & classical TS models (optional; used when tsfm_mode=chronos)
# timesfm  # Optional: Google TimesFM library; install if you want tsfm_mode=timesfm
torch>=2.4.1
arch>=6.3
transformers>=4.44.0
statsmodels>=0.14.2
torch-directml>=0.2.0; platform_system == "Windows"  # Windows GPU fallback backend (DirectML)

# Testing
pytest>=8.0.0

# Linting / Formatting (optional: set up later)
black>=24.0.0
ruff>=0.5.0
requests>=2.32.3
matplotlib>=3.8.0

# --- Notes ---
# Pinned core runtime libs above to ensure reproducible environment.
# Consider generating a full lock (pip freeze > requirements.lock) for deployments.
